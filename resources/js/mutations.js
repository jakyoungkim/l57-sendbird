export default {
    item(state, data) {
        state.item = data
    },
    createPopup(state, data) {
        state.createPopup = data
    },
    popupTitle(state, data) {
        state.popupTitle = data
    }
}
